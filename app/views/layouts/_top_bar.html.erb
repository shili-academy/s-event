<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <div class="container-fluid">
    <%= link_to "S-event", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "User Dashboard", root_path, class:"nav-link active fw-bold", "aria-current": "page" %>
        </li>
        <li><%= link_to "Admin Dashboard", admin_root_path, class: "nav-link" if current_user.admin? %></li>
        <%= render "layouts/dropdown" %>
      </ul>
      <%= search_form_for @q, url: root_path,
        class: "d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search" do |f| %>
        <div class="input-group">
          <%= f.search_field :name_or_description_cont, class: "form-control bg-light border-0 small",
            placeholder: t("navbar.search_event") %>
          <%= button_tag(type: :submit, class: 'btn btn-primary') do %>
            <i class="fas fa-search fa-sm"></i>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
